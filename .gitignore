# Houdini auto-backup dirs (bak1..bakN copies on every HDA save/build)
houdini/backup/
houdini/hda/backup/
__pycache__/
*.pyc

# transient camera+light sidecar written by render_usd_fleet.py (embeds abs path)
*.cam.usda
